# Configurating connection parameters for using MySQL Database #
dbname: raddict
username: raddict
password: raddict
params:
  host: localhost 
  port: 3306
  dialect: mysql
  storage: ./database/posts-sequelize.mysql # giving the database filename to use
  operatorsAliases:
    $eq: Op.eq
    $ne: Op.ne
    $gte: Op.gte
    $gt: Op.gt
    $lte: Op.lte
    $lt: Op.lt
    $or: Op.or
